@model List<RaceDayDisplayApp.Models.RaceStatistics>

@{
    Layout = null;
}


@if (Model.Count() == 0)
{
    <h4>No data available for this combination of filters</h4>
}
else
{
    <table class="stats-table">
        <thead>
            @*TODO: How am I gonna hide the header?*@
            @Html.DisplayFor(m => m[0], "RaceStatisticsHeader")
        </thead>
        <tbody>
            @Html.DisplayFor(m => m)
        </tbody>
    </table>
}